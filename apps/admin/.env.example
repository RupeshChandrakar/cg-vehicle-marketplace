# Copy to .env.local for local development.
NEXT_PUBLIC_API_URL=http://localhost:4000
# Where the customer-facing web app is deployed — used to build shareable
# vehicle links in admin-side tools (e.g. the WhatsApp new-listings digest).
NEXT_PUBLIC_CUSTOMER_WEB_URL=http://localhost:3000
